export type UserInformation = {
    [firstName: string]: string;
    lastName: string;
    email: string;
    city: string;
    phone: string;
}


export type StringObject = {
    [key: string]: string;
}